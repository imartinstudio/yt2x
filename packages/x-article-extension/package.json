{
  "name": "x-article-extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 extension to import Markdown into X Articles draft editor.",
  "scripts": {
    "build": "node ./build.mjs",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@yt2x/core": "workspace:*",
    "mermaid": "^11.6.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
