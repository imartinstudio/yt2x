{
  "name": "x-following-extension",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 extension to filter, select, and bulk-unfollow on X following lists.",
  "scripts": {
    "build": "node ./build.mjs",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
