# 复制为 .env 后填写（.env 已被 .gitignore 忽略，勿提交密钥）
#
# ── 默认走哪家 LLM（省略 `pnpm yt2x notes` / `article` / `llm ping` 的 --llm-provider 时生效）
# 取值：openai | anthropic | deepseek | moonshot
# 别名：gpt → openai，claude → anthropic，kimi → moonshot
YT2X_LLM_PROVIDER=openai
# 与上一行二选一即可（优先读 YT2X_LLM_PROVIDER）
# YT2X_DEFAULT_LLM_PROVIDER=deepseek

# ── OpenAI / ChatGPT
# OPENAI_API_KEY=
# OPENAI_KEY=
# GPT_API_KEY=

# ── Anthropic Claude
# ANTHROPIC_API_KEY=
# CLAUDE_API_KEY=
# CLAUDE_KEY=

# ── DeepSeek
# DEEPSEEK_API_KEY=

# ── Moonshot（Kimi）
# MOONSHOT_API_KEY=

# 可选：覆盖默认模型或 Base URL（一般不必）
# OPENAI_BASE_URL=https://api.openai.com/v1

# ── X（Twitter）OAuth 2 —— `pnpm yt2x auth login` / `publish` 真发需要
#
# 获取方式（网页登录用 PKCE，一般只需 Client ID）：
#   1. 打开 https://developer.x.com/en/portal/dashboard （或旧版 developer.twitter.com）
#   2. 选中你的 Project → App →「User authentication settings」里启用 OAuth 2.0
#   3. App type 选「Native App」或「Web App」均可；记下 **OAuth 2.0 Client ID**（一串字母数字）
#   4. Callback / Redirect URI 须包含本文件末尾 `X_REDIRECT_URI` 的地址（本地默认端口 8989）
#
# 下一行「你的client_id」为占位符：复制本文件为 `.env` 后，务必改成 Portal 里的真实 OAuth 2.0 Client ID，再执行 `pnpm yt2x auth login`。
# （占位字符串无法通过 X 鉴权，属预期行为。）
X_CLIENT_ID=你的client_id

# 若应用在 Portal 为 Confidential，取消注释并填写 Secret（公开 Native / PKCE 多数可留空）：
# X_CLIENT_SECRET=你的client_secret

# 与 Portal「Callback URI」完全一致；不设置时 CLI 默认：
X_REDIRECT_URI=http://127.0.0.1:8989/callback
