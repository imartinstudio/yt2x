{
  "name": "x-article-extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Chrome MV3 extension to import Markdown into X Articles draft editor. 将本地 Markdown 导入 X Articles 草稿编辑器。",
  "scripts": {
    "build": "node ./build.mjs",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@yt2x/core": "workspace:*",
    "mermaid": "^11.6.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.304",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3"
  }
}
