# 双语字幕 + 中文配音：架构方案

> 本文取代 2026-07-30 版《中文配音任务设计》。旧版把配音设计成一条独立交付路径，并在
> 决策 #5 / #8 中锁定「只烧中文字幕」「只出配音版」——这两条框定错误导致配音自建了一套
> 与双语字幕并行的交付链路，是此后大量缺陷的共同来源。本文重新定义交付物。

## 交付物

**一个视频**：原片画面 + 中文配音音轨（保留 BGM）+ 中英双语硬字幕，字幕与配音语音精确对齐。

配音是**双语字幕交付的一个可选增强**，不是独立产物。关掉配音时走现有双语字幕交付；打开时
把音轨换成中文、字幕跟着配音时间轴重计时，**只烧一次**。

## 核心取舍（已确认）

中文比英文普遍长 10–30%，三者不可兼得：

|                | 取舍                                       |
| -------------- | ------------------------------------------ |
| ① 锁死原片时长 | **选定**                                   |
| ② 语速自然均衡 | 保留，靠前置吸收让它极少被触发             |
| ③ 译文信息完整 | 在翻译阶段以「凝练」形式让步，而非事后砍削 |

选 ① 的理由：素材是讲者出镜与录屏交替的混合形态，冻结画面在出镜段落非常明显；而全程
1.2 倍以上语速在长视频上疲劳感强。

**关键**：③ 的让步做对了几乎无损。英文口语冗余度高（填充词、同义反复、换个说法再说一遍），
带时长预算翻译时这些先被挤掉，实质内容基本不动。当前实现测得的 44% 信息损失，是「先自由
翻译再让 LLM 事后砍」的顺序错误造成的，不是这个取舍的固有代价。

## 时长吸收的四层顺序

必须按序吸收，前层做对则极少走到后层：

1. **长度受限翻译** —— 翻译时即带「本句可用 N 毫秒」预算，挤掉口语冗余
2. **利用原片句间静音** —— 说话人换气的空档是现成余量
3. **轻微调速 0.95–1.15** —— 吸收残余，听感无损
4. **压缩内容** —— 兜底，前三层做对后应极少触发

当前实现跳过了第 1、2 层，直接从第 4 层开始，这是质量问题的根源。

## 两级切分

解决「字幕要短」与「分句要自然」的表面矛盾：

```
话语单元 utterance   ← TTS 合成单位；来自源语音的换气停顿，不是字幕行
        ↓ 合成后得到该句的实测起止
显示单元 cue         ← 在话语单元内部按屏宽再切，时间按字符比例分配
```

因为显示单元是话语单元的**细分**，而话语单元的实际时长是**实测值**，四项要求同时成立：
字幕短、分句自然、不漂移（误差不跨句累积）、中英同源同时间轴。

## 数据流

```
原片（downloads，只读）
  ├─ 音轨 ─→ ASR 词级时间戳（faster-whisper，已有）
  │            ↓
  │        话语切分（按停顿聚类）          ← 新增
  │            ↓
  │        长度受限翻译（带时长预算）       ← 新增，取代事后压缩
  │            ↓
  │        TTS 合成（带时间戳，已有）
  │            ↓
  │        时长吸收（静音 + 调速，改造）
  │            ↓
  │        中文音轨 + 时间轴映射 T→T′
  │
  └─ 人声分离（Demucs，已有）─→ BGM 轨
                ↓
        混音（中文人声 + BGM）
                ↓
  双语字幕按 T′ 重计时（中文=配音稿，英文=同话语单元原文）
                ↓
        换音轨 + 烧一次 → 成片
```

**配音不再自己烧片。** 它交出音轨与时间轴映射，由双语交付路径统一烧录。

## 现有代码处置

### 保留

| 模块                                                                           | 说明                             |
| ------------------------------------------------------------------------------ | -------------------------------- |
| `dub/demucs.ts`                                                                | 人声分离                         |
| `dub/edge-tts.ts`、`dub/edge-tts-subtitles.ts`                                 | TTS + 词级时间戳                 |
| `dub/elevenlabs.ts`                                                            | 维持当前「未验证即失败关闭」状态 |
| `dub/synthesize.ts`                                                            | 合成与时间戳落地                 |
| `dub/source-guard.ts`、`dub/file-store.ts`                                     | 素材契约、产物读写               |
| `core/dub/gate.ts`                                                             | 质量门禁（指标随新链路调整）     |
| `acquire/render-bilingual-subtitles.py`、`acquire/burn-bilingual-subtitles.ts` | 双语渲染与烧录，成为唯一烧录路径 |
| `acquire/transcribe-local.py`                                                  | 已能产出词级时间戳               |

### 改造

| 模块                                        | 改成什么                                                                    |
| ------------------------------------------- | --------------------------------------------------------------------------- |
| `core/dub/prompts.ts`                       | 从「朗读化改写」改为**长度受限翻译**：输入英文原文 + 可用时长，输出凝练中文 |
| `dub/script.ts`                             | 输入从字幕文件改为**话语单元**，不再合并字幕行                              |
| `core/dub/negotiate.ts`、`dub/negotiate.ts` | 保留调速与静音吸收；**移除激进的事后改短分支**，该职责上移到翻译            |
| `core/dub/reverse-srt.ts`                   | 从「生成中文单语 SRT」改为**在话语单元内细分显示单元、生成双语内容**        |
| `dub/remix.ts`                              | 保留混音与冻结帧能力；**移除自建烧录**，改为交出音轨与时间轴映射            |

### 废弃

| 模块                          | 原因                                                                                           |
| ----------------------------- | ---------------------------------------------------------------------------------------------- |
| `core/dub/segment.ts`         | 职责是「把字幕行合并回自然句」。新链路直接从语音切出自然句，这一步不存在；相邻去重逻辑随之消失 |
| `core/dub/shorten-prompts.ts` | 事后压缩被长度受限翻译取代                                                                     |

### 新增

| 模块                         | 职责                                           |
| ---------------------------- | ---------------------------------------------- |
| 话语切分（core，纯函数）     | 输入词级时间戳，按停顿阈值聚类成话语单元       |
| 显示单元细分（core，纯函数） | 在话语单元内按屏宽切分，按字符比例分配时间     |
| 双语内容组装                 | 中文取配音稿、英文取同话语单元原文，按 T′ 输出 |

## Issue 处置

| #                         | 处置                                                               |
| ------------------------- | ------------------------------------------------------------------ |
| #119 配音双语字幕         | **关闭** —— 被本架构自然吸收，不再是独立工作                       |
| #109 上游中文字幕紧邻重复 | **保留但与配音解耦** —— 配音不再读该文件；它仍是字幕产物自身的缺陷 |
| #110 落点报告来源标记     | 保留，小改动，排最后                                               |
| #118 Python 依赖未声明    | 保留，与本架构无关                                                 |
| info-loss 44%             | **不单独立项** —— 由长度受限翻译从根上消除                         |

## 落地顺序

分四个 PR，每个可独立验收：

1. **话语切分** —— 从词级时间戳产出话语单元；纯函数 + 单测；不改动现有链路
2. **长度受限翻译** —— 新 prompt + 预算参数；与旧路径并存以便对比
3. **切换链路** —— 配音改为消费话语单元；废弃 `segment.ts` / `shorten-prompts.ts`
4. **统一交付** —— 配音退出烧录，双语路径按 T′ 重计时并烧一次

前两个 PR 不影响现有行为，可安全先行。

## 验收标准（四维度，缺一不可）

此前多次出现「测试全绿而成片是坏的」，原因是每次只测了一个维度：

| 维度     | 判据                                               |
| -------- | -------------------------------------------------- |
| 起点     | 每条显示单元的起点与其语音起点偏差在容差内         |
| 终点     | 语音未被截断；时间戳推出的结束点不超过音频实际时长 |
| 停顿     | 相邻话语单元之间保留非零间隔                       |
| 字幕内容 | **画面上实际出现的是双语**，且中文与听到的一致     |

第四条必须**抽帧目视确认**。此前「字幕文件逐行精确但压根没被烧进画面」的缺陷，任何只校验
文件的断言都发现不了。

**单测全绿不构成验收通过**：必须用 30 秒窗口跑真实素材并按四维度实测，最终由人耳确认听感。

## 未决

- 最小句间停顿默认值（当前 150ms）需在新链路上重新试听确定
- 门禁阈值需在新链路跑通后以真实正片重新标定
- ElevenLabs 时间戳能力仍未经真实调用验证，接入前必须先验证
