# 术语表

本文件只定义领域词汇，不放实现细节。实现方案见 `docs/` 下对应文档。

## 字幕通道

字幕有五条**互斥**的来源通道，选定通道决定了这个视频能交付什么：

| 通道                 | 标识符        | 字幕来源                      | 词级时间戳 | 能配音 |
| -------------------- | ------------- | ----------------------------- | ---------- | ------ |
| **YouTube 通道**     | `youtube`     | 视频自带的现成字幕            | 无         | 否     |
| **句级内联转录通道** | `transcribe`  | whisper-cli 本地转写，整句    | 无         | 否     |
| **句级预转录通道**   | `local`       | faster-whisper 本地转写，整句 | 无         | 否     |
| **词级预转录通道**   | `local-words` | faster-whisper 本地转写，逐词 | 有         | **是** |
| **外部字幕通道**     | `file`        | 用户提供的字幕文件            | 无         | 否     |

配音必须知道每个词在第几毫秒被说出来，因此**「配音」只存在于词级预转录通道**。其余四条
通道的时间轴精度只到句，无法用来切分自然句、也无法让配音与画面同步。

「本地转录」不是一条通道，是三条，边界不能互相替代：

- `transcribe` 用 whisper-cli，随 `subtitle` 命令内联执行，写入跟 YouTube 通道相同的
  `full.<lang>.srt`。它不是备用选项——`--subtitle-source auto` 在双语流程下找不到 YouTube
  字幕时，会静默回退到这条通道，是当前实际跑得最多的本地转录路径。
- `local` 与 `local-words` 都由 faster-whisper 产出，来自同一次独立命令
  `yt2x subtitle transcribe-local`：句级结果写 `full.local.<lang>.srt`（`--subtitle-source
local` 读取），逐词结果写 `full.local.<lang>.words.json`。`local-words` 因此**不是**
  `--subtitle-source` 的一个取值——它没有选择入口，只被 `dub` 命令直接读取，`dub` 本身也不接受
  `--subtitle-source`。

把三条本地转录路径当成一回事会得出「本地转录就能配音」这个错误推论——只有 faster-whisper
产出的词级时间戳能驱动配音，whisper-cli 那条句级路径永远不能。

走词级预转录通道时，字幕与配音是同一次生成的产物，一起交付为一个带配音的视频。

## 话语单元（utterance）

从源语音的换气停顿切出的自然句，是**配音合成的单位**。它不是字幕行——一个话语单元可能对应
屏幕上连续的好几行字幕。

## 显示单元（cue）

屏幕上实际显示的一条字幕，是**话语单元的细分**：在话语单元内部按屏幕宽度再切开，时间按字符
比例分配。

因为显示单元始终从属于某个话语单元，而话语单元的时长是实测值，所以字幕不会随着视频推进
累积漂移——误差被关在单个话语单元内部。

## 交付物

一次视频交付产出**一样东西**，共六档，互斥：

| 档位               | 交付物                                              |
| ------------------ | --------------------------------------------------- |
| `none`             | 只取材，不产出字幕                                  |
| `zh-srt`           | 中文字幕文件                                        |
| `zh-burned`        | 中文硬字幕成片                                      |
| `bilingual-srt`    | 中英双语字幕文件                                    |
| `bilingual-burned` | 中英双语硬字幕成片                                  |
| `dubbed`           | 原片画面 + 中文配音音轨（保留背景音乐）+ 双语硬字幕 |

档位之间是阶梯：高档位包含低档位的产物。

配音是**双语字幕交付的可选增强**，不是独立产物——它是这条阶梯的最高一档，而不是一个与字幕
正交的开关。关掉配音走纯字幕交付；打开配音则换掉音轨、字幕按配音时间轴重新计时，**整个交付
只烧录一次**。
